<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="br.com.campuscode.contacts">


    <!--TODO vamos trocar o icone default do android por um customizado. Voce pode encontrar um zip na pasta assets, esta localizada na raiz do repositorio. -->
    <!--TODO O icone do aplicativo é o unico que fica em umas pasta separada dos outros assets, vamos estudar um pouco? -->
    <!--TODO verifique a documentação oficial do android para entender como ter um bom icone e pq temos diversos arquivos. -> https://developer.android.com/guide/practices/ui_guidelines/icon_design_launcher.html  -->
    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>